#!/usr/bin/env ruby
# Get the argument from the command line
input = ARGV[0]

# Define the regular expression
regex = /hb{1,5}tn/
match = input.match(regex)

if match
  puts "#{match[0]}"
else
  puts ""
end

input = ARGV[0]
regex = /School/
match = input.match(regex)

# Output the matched word
if match
  puts "The word matched is: #{match[0]}"
else
  puts "The input does not contain the word 'School'"
end
